# Ray Tracer
Creates images (.png) using objects set in a scene and applying ray tracing to them. There are 7 different scenes.

# Build
mkdir build
cd build
cmake ../src
make -j

# Run
./A6 scene image_size image_filename
